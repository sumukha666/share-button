import React from "react";
import UserProfile from "../../common/UserProfile";

const Header = () => {
  return (
    <div>
      <UserProfile
        title="Share to web"
        info="publish and share link with anyone"
      />
    </div>
  );
};

export default Header;
